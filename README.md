# Expense Tracker API

Simple REST API built with Node.js, Express, and SQLite.

## Endpoints

- POST /expense_tracker
- GET /expense_tracker
- PUT /expense_tracker/:id
- DELETE /expense_tracker/:id

## Run

npm install  
npm run dev
